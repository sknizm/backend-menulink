const express = require('express');
const router = express.Router();

// Routes
// router.get('/', );

// router.get('/id/:email', userController.getUserIdByEmail);
// router.get('/current', userController.getCurrentUserFromSession);
// router.get('/current-id', userController.getUserIdFromSession);

module.exports = router;
